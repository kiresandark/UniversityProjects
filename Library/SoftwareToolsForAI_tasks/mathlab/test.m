x = 0.5; 
w = 0.8; 
b = -0.1; 
% Функция активации 
sigmoid = @(x) 1 ./ (1 + exp(-x)); 
% Вычисление выходного значения y = sigmoid(w * x + b); 
% Вывод результата disp(y); 
% Эксперименты с функцией активации и настройками весов 
w = -0.8; 
% изменение веса y = 
sigmoid(w * x + b); 
disp(y); 
b = 0.1; % изменение смещения 
y = sigmoid(w * x + b); 
disp(y); 
sigmoid = @(x) max(0, x); 
% ReLU функция активации 
y = sigmoid(w * x + b); 
disp(y);